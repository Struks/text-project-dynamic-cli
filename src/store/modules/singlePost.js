// import db from '@/firebase/init'
const state = {
    // title:'',
    // url:'',
    // text:'',
    // timestamp:'',
    // headline: '',           
    // id:'',
}

const getters = {
    // title:(state) => state.title,
    // url:(state) => state.url,
    // text:(state) => state.text,
    // timestamp:(state) => state.timestamp,
    // id:(state) => state.id,
    // headline:(state) => state.headline,

}

const mutations = {


}

const actions = {
//     getSinglePost(payload){
//     db.collection('blog').doc(payload).get().then(doc =>{
//         if(doc.exists){
//             state.title = doc.data().title;
//             state.url = doc.data().url;
//             state.text = doc.data().text;
//             state.timestamp = doc.data().timestamp;
//             state.headline = state.title; 
//             state.id = doc.id;
//         }
//     })
// }

}

export default{
    state,
    getters,
    mutations,
    actions
}