// import db from '@/firebase/init';

const state ={
    fireForm:{
        username:'',
        password:'',
    }
}

const getters ={
    fireForm: state => state.fireForm
}

const mutations ={

}

const actions ={

}

export default{
    state,
    getters,
    mutations,
    actions
}